# Issue #47 Close Note

Issue #47 defines the Home Assistant staged test plan.

Completed work:

- `docs/home-assistant-staged-test-plan.md` added
- `docs/next-issue.md` updated for Stage 2

Result:

- Stage 1 remains fake/mock data.
- Stage 2 is empty local Home Assistant.
- Stage 3 is one harmless test entity.
- Stage 4 is read-only real home status.
- Stage 5 is private dashboard wiring.
- Stage 6 is controlled actions later.

Next PC-only action:

```text
Start Docker Desktop, then run docker ps.
```
